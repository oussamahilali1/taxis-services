# Frontend build-time variable
# Set this on Vercel if your API is deployed on a different domain.
# Leave it empty when the frontend can reach the API on the same origin.
PUBLIC_API_BASE_URL=
PUBLIC_CAPTCHA_MODE=off
PUBLIC_CAPTCHA_PROVIDER=
PUBLIC_CAPTCHA_SITE_KEY=

# Backend runtime variables live in backend/.env
# Copy backend/.env.example to backend/.env for local development.
